version: '3.8'

services:
  mod-collab:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mod-collab
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      MOD_COLLAB_CORS: ${MOD_COLLAB_CORS:-true}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:5173}
      GATEWAY_TRUST_TOKEN: ${GATEWAY_TRUST_TOKEN}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - cima-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3001/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  postgres:
    image: postgres:16-alpine
    container_name: mod-collab-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-cima_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-cima_password}
      POSTGRES_DB: ${POSTGRES_DB:-cima_crm}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./drizzle:/docker-entrypoint-initdb.d
    networks:
      - cima-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-cima_user} -d ${POSTGRES_DB:-cima_crm}"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  cima-network:
    driver: bridge

volumes:
  postgres-data:
    driver: local
